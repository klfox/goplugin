FROM golang:latest

ARG LOWERVERSION
ARG UPPERVERSION

ENV LOWER_GOVERSION ${LOWERVERSION}
ENV UPPER_GOVERSION ${UPPERVERSION}

COPY run.sh .

RUN chmod -R 777 run.sh

CMD ./run.sh $LOWER_GOVERSION $UPPER_GOVERSION
